const fs = require("fs");
// fs.appendFile("file_name.ext", "Hello World", (error) => {}); // Create a file or existed file and add something in it
// fs.writeFile("file_name.ext", "Hello World", (error) => {}); // Create a file or overwright file and write something in it
// fs.readFile("file_name.ext",  (error, data) => {}); // Display the content of the file as buffer data.
// fs.readFile("file_name.ext", "utf-8", (error, data) => {}); // Display the content of the file as text data.
// fs.rename("file_name.ext", "example_renamed.txt", (err) => {}); // Rename the existed file.
// fs.unlink("file_name.ext", (err) => {}); // Delete the existed file.
// fs.mkdir("folder_name", (err) => {}); // Create a folder.
// fs.rmdir("folder_name", (err) => {}); // Delete a folder.
// fs.readdir("folder_name", (err, files) => {}); // Display all files in the folder.
// fs.open("file_name.ext", "w", (err, file) => {}); // Create a file.
// fs.close(file, (err) => {}); // Close the file.
// fs.existsSync("file_name.ext or dir name"); // Check if the file exists or not.
// fs.stat("file_name.ext or dir name", (err, stats) => {}); // Display the status of the file.
